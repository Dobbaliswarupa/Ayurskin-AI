[project]
name = "ayurskin-ai"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "pillow",
    "numpy",
    "opencv-python-headless",
    "python-dotenv",
    "google-genai"
]

[tool.vercel]
entrypoint = "backend.main:app"